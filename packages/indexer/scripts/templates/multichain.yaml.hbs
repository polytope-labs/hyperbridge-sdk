specVersion: 1.0.0
{{#if parent}}
parent:
  untilBlock: {{parent.untilBlock}}
  reference: '{{parent.CID}}'
{{/if}}
query:
  name: '@subql/query'
  version: '*'
projects:
{{#each projects}}
  - {{this}}
{{/each}}
