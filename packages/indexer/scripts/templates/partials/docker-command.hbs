- ${SUB_COMMAND:-}
{{#if hasCid}}
- -f={{cid}}
{{else}}
- -f=/app/{{chainName}}.yaml
{{/if}}
- --db-schema=app
- --workers=${SUBQL_WORKERS:-16}
- --batch-size=${SUBQL_BATCH_SIZE:-100}
- --multi-chain
- --unsafe
- --log-level=info
- --historical=timestamp
- --allow-schema-migration
{{#if unfinalizedBlocks}}
- --block-confirmations=0
{{/if}}
- --store-cache-async=true
