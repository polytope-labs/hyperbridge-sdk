{{#each handlers}}
  - handler: {{this.handler}}
    kind: {{../handlerKind}}
    filter:
    {{#if this.module}}
      module: '{{this.module}}'
      method: '{{this.method}}'
    {{else}}
      topics:
        - '{{this.topic}}'
    {{/if}}
{{/each}}
